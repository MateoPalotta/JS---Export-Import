// Función para sumar dos números
function sum(a, b) {
    return a + b;
}

// Función para restar dos números
function restar(a, b) {
    return a - b;
}

// Exportar las funciones
export { sum, restar };
